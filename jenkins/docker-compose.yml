services:
  jenkins:
    build: .
    container_name: jenkins-ci
    ports:
      - "8084:8080"
      - "50000:50000"
    volumes:
      - jenkins_data:/var/jenkins_home
      - /home/vova/.ssh:/var/jenkins_home/.ssh:ro
    restart: always

volumes:
  jenkins_data:
